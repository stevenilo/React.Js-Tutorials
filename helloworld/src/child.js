//import react js functional component with using React, {component}
import React from "react";

//declaring Child() component
function Child() { 
    //returning html tag
    return <h1>This is Child component (child.js)</h1>
 }

 //exporting the class Child to app.js
 export default Child;